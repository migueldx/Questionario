@model IEnumerable<Questionario_Agrotools.Models.Pergunta>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Questionario.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescricaoPergunta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescricaoResposta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Questionario.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescricaoPergunta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescricaoResposta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PerguntaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PerguntaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PerguntaId })
        </td>
    </tr>
}

</table>
